# === Core ===
streamlit>=1.40.0
pandas>=2.2.0
numpy>=1.26.0
plotly>=5.20.0
requests>=2.31.0
pyarrow>=15.0.0            # snapshot parquet

# === Modelli quantitativi ===
arch>=6.3.0                # EGARCH per la realized vol attesa (VRP)
scipy>=1.12.0              # Spearman nella validazione della gamba VRP

# === Operativita' ===
pandas-market-calendars>=4.4.0   # freshness guard (opzionale: fallback su busday)

# Note:
#  - I dati arrivano da CBOE (CSV pubblici, nessuna chiave) e da EODHD (chiave in
#    EODHD_API_KEY, serve solo alla pipeline). La dashboard NON fa fetch e non ha
#    bisogno di alcun secret.
#  - Nessun modulo in src/ importa Streamlit tranne charts.py: gli stessi calcoli
#    girano identici nella GitHub Action e nella dashboard.
#  - yfinance NON e' piu' una dipendenza: la ridondanza e' data dalla coppia
#    CBOE + EODHD, che il cross-check ha dimostrato coincidenti.
